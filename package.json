{
  "name": "cli-currency-converter",
  "version": "1.0.0",
  "description": "The TypeScript console app is used to convert currencies: the users enter a certain amount of money in one currency and set the currency they want to check the monetary value of.\r While developing the app, the beginners can master variables, algorithms, loops, if statements, and other TypeScript concepts.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^9.3.3"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7"
  }
}
